# Validation tools only; install alongside the checked-out requirements.txt.
# Reuse that source's constraints-lambda.txt when it exists.
-r requirements-dev.txt
pytest==9.1.1
PyYAML==6.0.3
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
