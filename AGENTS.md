# 작업 지침

이 저장소의 최신 사용자 지시를 우선한다. 문서의 과거 제안, AI 검토 의견, 현재 코드 동작을 사용자 확정 결정으로 바꾸지 않는다.

## 먼저 읽을 문서

1. `README.md`: 현재 상태와 핵심 문서 9개의 위치.
2. `docs/DECISIONS.md`: 유지해야 할 결정, 정정된 과거 방침, 미정 질문.
3. 변경 대상에 맞는 `docs/ARCHITECTURE.md`, `docs/ARC_MOCK_IMPLEMENTED_API_CONTRACT_KO.md`, `docs/LOCAL_RUN.md`, `docs/DEPLOY_GUIDE.md`, `docs/VALIDATION.md`.

앱팀용 요청·응답 명세는 `docs/APP_API.md` 한 곳에 Markdown으로 유지한다. 계산 파서·계산 JSON과 기존 API는 상세 API 계약을 확인한다. 코드와 결정이 다르면 차이를 보고하고, 정책을 임의로 코드에 맞추지 않는다.

## 보존할 경계

- 기존 내부 계산기·파서·응답·자료형·null·코칭과 승인된 ARC 최소량 정책을 유지한다. 외부 계산 서비스를 추가하거나 폐기된 외부 Scoring 전용 코드·복사 소스를 되살리지 않는다.
- 앱의 실제 누적 CPR/AED 바이너리를 계산한다. Dummy는 로그인과 임시 프로그램용이며 가짜 점수를 뜻하지 않는다.
- 계산 성공, 프로그램 완료, 현재 공유 진도 반영, ARC 제출 성공을 구별한다. 미정 CPR 완료 규칙을 임의 공식으로 채우지 않는다.
- HSTM 호환 문서를 ARC 공식 제출 문서로 간주하지 않는다. 실제 HSTM 전송은 금지하며 ARC 계약 확보 전 제출 비활성을 유지한다.
- 인증·소유권·멱등성·epoch·lease/fence·비공개 파일·차트 서명·오류/로그 정제를 문서 정리나 연결 편의를 이유로 완화하지 않는다.
- 사용자 소유 서버·DB·참고 저장소·작업 중 변경을 보존한다. 시험은 별도 임시 설치·포트에서 수행한다. 저장 파일·키·골든 결과를 오류 해결 목적으로 임의 삭제/교체하지 않는다.
- 새 정책, 미정 계약, 실제 자원·운영값 또는 상충하는 해석은 사용자에게 질문한다. 이미 확정된 사항을 반복 질문하지 않고 답변과 무관한 승인 작업은 계속한다.
- 사용자 요청에 포함된 역할·인원·단계별 검토 절차를 따른다. 실제 수행하지 않은 상호 검토나 CTO 승인을 보고하지 않는다. AI 역할의 판정을 사람의 인증·운영 승인으로 표현하지 않는다.
- 현재 작업 범위에 맞는 검증을 실행한다. 계산/상태/네트워크 변경은 해당 회귀·실제 경계 시험을 수행하고, 로컬 시험·실물 앱·AWS·ARC 검증을 구분해 보고한다.

## 문서를 다시 늘리지 않는 방법

- 핵심 Markdown 9개를 갱신한다. 새 단계별 계획·개인 리뷰·CTO 리뷰 파일을 계속 추가하지 않는다. 결과는 검증 요약에 모으고 결정은 결정 문서 한 곳에 반영한다.
- 2026-09-18 정리 지시에 따라 과거 작업 사본·진행 기록·중복 문서를 제거했다. 최신 결과는 검증 요약에 남긴다. 필요한 임시 실행 증거만 `.documentation-backup/` 등에 두고 작업 종료 시 불필요한 기록을 정리하며 Git·배포물에 넣지 않는다.
- 2026-09-19 사용자 지시에 따라 앱 전달 계약은 YAML 대신 `docs/APP_API.md`에 유지한다. API를 변경하면 코드와 Markdown의 경로·필수값·자료형·null·요청/응답 예시를 함께 갱신한다.
- 코드/테스트가 읽는 계약·배포 파일과 회귀 fixture는 개발 진행 기록과 구별한다. `docs/local_server/DYNAMODB_DISTRIBUTION_MANIFEST.json`, `docs/implementation_execution/P4C_ROUTE_ROLE_MANIFEST.json`, `tests/fixtures/`, `tests/dataset/`의 필요한 자료를 기록물이라는 이유로 제외하지 않는다.
- DB 저장 대상은 앱의 훈련 진도·결과·운용 로그다. N03~N06에 따라 기존 DB 참조+비공개 파일 구조, 주요/정제된 상세 진단, 로그 장애와 훈련의 분리, 기간 확정 전 자동 삭제 없음을 유지한다. 개발 기록 JSON·patch를 DB 적재 대상으로 바꾸지 않는다. 로컬 기록기 구현을 AWS 로그 전달 완료로 해석하지 않는다.

## Git과 외부 작업

- commit·push·실제 배포는 사용자가 결정한다. 명시적 지시 없이 실행하지 않는다.
- 2026-09-10 문서 통합에서 승인된 원본의 Git 추적 해제는 그 정리 대상에만 한정된다. 일반 파일의 staging/unstaging 권한으로 확대하지 않는다.
- 비밀값·세션 토큰·복구 증표·차트 서명 URL을 코드, 문서, 명령 인자, 로그에 넣지 않는다. 원본 로컬 보관과 실제 DB 데이터도 Git/배포물에 넣지 않는다.
